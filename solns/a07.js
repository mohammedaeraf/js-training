const numbers = [4, 7, 12, 15, 9];
let modifiedNumbers = numbers.map((n) => (n % 2 == 0 ? n * 2 : n + 1));
console.log(modifiedNumbers);

const numbers2 = [12, 5, 8, 130, 44, 3, 25, 60, 7, 90];
evenNumbers = numbers2.filter((n) => n % 2 === 0);
console.log(evenNumbers);

