// square of 5 = 5 * 5 = 25

// Pattern
// Step 1 - Take Input
// Step 2 - Do some calculation or processing
// Step 3 - Give output

function calcSquare(n) {
  let sq = n * n;
  return sq;
}

let square = calcSquare(10);
console.log(square);
