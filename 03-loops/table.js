// Set the number for which the multiplication table will be printed
let n = 5;

// Loop from 1 to 10 to generate the multiplication table
for (let i = 1; i <= 10; i++) {
  // Print the current multiplication in the format "n x i = result"
  console.log(n + " x " + i + " = " + n * i);
}