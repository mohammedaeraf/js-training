// Initialize an empty string to store the odd numbers
let str = "";

// Loop from 1 to 30, incrementing by 2 each time to get only odd numbers
for (let i = 1; i <= 30; i = i + 2) {
  // Append the current odd number and a space to the string
  str = str + i + " "; // "1 3 5 ..."
}
// Print the resulting string of odd numbers to the