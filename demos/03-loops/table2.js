// Use template strings to print the multiplication table
let n = 5;

// Loop from 1 to 10 to generate the multiplication table
for (let i = 1; i <= 10; i++) {
  // Print the current multiplication using a template string for formatting
  console.log(`${n} x ${i} = ${n * i}`);
}