// Initialize variable x with the value 100
let x = 100;

// The do-while loop will execute the block at least once
do {
  // Print the current value of x to the console
  console.log(x);
  // Increment x by 1
  x++;
  // Continue looping as long as x is less than or equal to 10
} while (x <= 10);
